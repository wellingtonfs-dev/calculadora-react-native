import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    fontSize: 40,
    fontWeight: 'bold',
    textAlign: 'right',   
    color: '#ffffff',
    alignItems: 'flex-end',   
    margin: 10,
    padding: 20,
    borderWidth: 2,
    borderColor: 'white',
    borderRadius: 10,
    backgroundColor: '#4e4e4e',
    width: '100%',       
  }
});