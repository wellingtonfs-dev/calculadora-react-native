import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  line: {
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center",
    marginVertical: 8,
  },
});
